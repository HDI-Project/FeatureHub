FROM jupyterhub-docker:0.7.2

# Install additional packages
RUN /opt/conda/bin/pip install \
    'oauthenticator==0.5.*' \
    'dockerspawner==0.5.*' \
    'python-dateutil==2.6.*' \
    'flask>=0.12'
